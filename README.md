# 📊 Streamlit 前端应用文档

`main.py` 是一个基于 **Streamlit** 构建的用户友好的 Web 应用界面，提供一键式的投资报告生成体验。

---

## 功能概述

本应用提供完整的 GUI 界面，用户无需了解后端细节，只需点击按钮即可自动完成以下流程：

1. **连接 News Platform** - 从新闻平台获取最新市场分析文章
2. **下载素材** - 自动下载并整理文章配图
3. **AI 生成** - 调用 EasyView AI 进行深度分析与总结
4. **PPT 渲染** - 根据地区选择生成对应版本的 PowerPoint 报告
5. **一键下载** - 生成的 PPT 文件直接下载到本地

---

## 系统架构

```
┌──────────────────────────────────────────────────────────┐
│           Streamlit 前端界面 (stream.py)                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 密码认证层                                        │   │
│  │ └─ check_password()                               │   │
│  └──────────────────────────────────────────────────┘   │
│                     ▼                                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 设置界面                                          │   │
│  │ └─ 地区选择 (中国大陆 / 香港 / 新加坡)          │   │
│  └──────────────────────────────────────────────────┘   │
│                     ▼                                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 执行按钮 "开始生成 PPT"                          │   │
│  │ └─ 触发 4 阶段工作流                             │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└──────────────────────────────────────────────────────────┘
          ▼            ▼           ▼            ▼
      ┌────────────────────────────────────────┐
      │  调用后端模块:                          │
      ├────────────────────────────────────────┤
      │ • fetch_articles()                     │
      │ • json_main() (construct_json.py)      │
      │ • AIPromptRunner() (AI_prompt_ready.py)│
      │ • generate_ppt_from_json() (ppt_ready)│
      └────────────────────────────────────────┘
```

---

## 快速开始

### 1. 安装依赖

```bash
pip install streamlit requests
```

### 2. 配置 config.py

编辑 `config.py`，确保以下参数已设置：

```python
# 密码设置
APP_PASSWORD = "your_password_here"

# News Platform 配置
NEWS_AUTH_URL = "https://api.newsplatform.com/oauth/token"
NEWS_CLIENT_ID = "your_client_id"
NEWS_CLIENT_SECRET = "your_client_secret"
NEWS_ARTICLE_URL = "https://api.newsplatform.com/articles"

# AI 服务配置
AI_API_KEY = "your_ai_api_key"
AI_API_URL = "https://ai.easyview.com/api"

# 模板文件配置
TEMPLATE_MAP = {
    "中国大陆": "./template/mainland_template.pptx",
    "香港": "./template/hongkong_template.pptx",
    "新加坡": "./template/singapore_template.pptx"
}

# 输出目录
OUTPUT_DIR = "./ai_generate"
```

### 3. 启动应用

```bash
streamlit run stream.py
```

应用将在 `http://localhost:8501` 打开（Streamlit 默认端口）

---

## 用户界面说明

### 1. 登录界面

<img alt="login" src="docs/login_screen.png" />

- **访问密码**: 在 `config.py` 中设置 `APP_PASSWORD`
- 首次访问需要输入密码，验证通过后会话保持

### 2. 主要控制面板

**设置区 (Section 1)**：
```
请选择 PPT 目标地点:
○ 中国大陆  ○ 香港  ○ 新加坡
```

- 选择生成的 PPT 针对哪个地区
- 系统会自动切换对应的模板文件

**执行区 (Section 2)**：
```
🚀 开始生成 PPT [点击按钮]
```

- 点击按钮启动完整的生成流程
- 进度条显示实时进度 (10% → 30% → 50% → 80% → 100%)

### 3. 生成进度显示

生成过程中，界面会显示分步进度：

```
Step 1/4: 连接 News Platform 获取数据...          [████░░░░░░░░░░░░░] 10%
Step 2/4: 下载图片并整理素材...                    [██████████░░░░░░░░] 30%
Step 3/4: AI 正在撰写报告 (需约 1-2 分钟)...      [████████████░░░░░░] 50%
Step 4/4: 正在渲染 香港 版 PPT...                 [██████████████░░░░] 80%
✅ PPT 生成完成！                                    [██████████████████] 100%
```

### 4. 下载按钮

生成成功后，会显示下载按钮：

```
📥 点击下载: AI_PPT_generated_香港.pptx
```

- 一键下载生成的 PPT 文件
- 文件自动命名为 `AI_PPT_generated_{地区}.pptx`

---

## 核心功能模块

### 密码验证 (check_password)

```python
def check_password():
    """简单的密码保护"""
    # 保存在 session_state 中，实现单会话记忆
    # 密码正确时返回 True，允许访问主应用
```

**作用**：
- 保护应用访问权限
- 使用 Streamlit 的 `session_state` 机制实现会话级别的认证

### 获取平台 Token (get_news_platform_token)

```python
def get_news_platform_token():
    """获取 News Platform Token (使用 config.py 配置)"""
    # 使用 OAuth 2.0 Client Credentials 流程
    # 返回 access_token 用于后续 API 调用
```

**参数**（来自 config.py）：
- `NEWS_CLIENT_ID`: 客户端 ID
- `NEWS_CLIENT_SECRET`: 客户端密钥
- `NEWS_AUTH_URL`: Token 获取端点

**返回值**：
- 成功：`access_token` 字符串
- 失败：`None`

### 抓取文章 (fetch_articles)

```python
def fetch_articles(token):
    """抓取文章列表"""
    # 使用 Bearer Token 认证
    # 返回 News Platform 的最新文章 JSON
```

**参数**：
- `token`: 从 `get_news_platform_token()` 获取

**返回值**：
- 成功：文章列表 JSON
- Token 失效：`"EXPIRED"`
- 失败：`None`

### 获取模板路径 (get_template_path)

```python
def get_template_path(location_name):
    """根据 config 映射获取模板路径"""
    # 根据地区名称查表获取对应的 .pptx 模板文件
```

**参数**：
- `location_name`: 地区名称（"中国大陆"、"香港"、"新加坡"）

**返回值**：
- 模板文件的绝对路径

### 主应用流程 (main_app)

生成 PPT 的 4 个阶段：

#### Stage 1: 获取数据 (10% → 30%)
```python
token = get_news_platform_token()
articles = fetch_articles(token)
save_temp_json(articles, 'articles.json')
```

#### Stage 2: 处理素材 (30% → 50%)
```python
articles_dir, images_dir = json_main("articles.json")
# json_main() 完成：
# • 解析 JSON 结构
# • 下载配图到本地
# • 整理目录结构
```

#### Stage 3: AI 生成 (50% → 80%)
```python
runner = AIPromptRunner()
final_json_data = runner.run(specific_folder=articles_dir)
# AIPromptRunner 完成：
# • 提交内容到 AI
# • 轮询等待结果
# • 保存为 final_investment_report.json
```

#### Stage 4: 渲染 PPT (80% → 100%)
```python
template_path = get_template_path(location_name)
success = generate_ppt_from_json(
    report_path, 
    template_path, 
    output_filename, 
    location_name, 
    images_dir
)
```

---

## 错误处理与调试

### 常见错误信息

| 错误信息 | 原因 | 解决方案 |
|--------|------|---------|
| "无法获取 News Token，请检查 config.py" | Token 获取失败 | 检查 `NEWS_CLIENT_ID` 和 `NEWS_CLIENT_SECRET` |
| "文章列表为空或 Token 失效" | News Platform 返回空或 Token 过期 | 重新运行或检查 API 可用性 |
| "文件处理失败，无法生成文章目录" | `construct_json.py` 异常 | 查看后台日志 |
| "AI 生成失败，请查看后台日志" | AI 服务异常 | 检查 `AI_API_KEY` 和 `AI_API_URL` |
| "错误：找不到生成的文件" | 输出目录权限问题 | 确保 `OUTPUT_DIR` 可写 |

### 启用详细日志

在 `stream.py` 顶部已配置日志：

```python
logging.basicConfig(
    level=logging.INFO, 
    format='%(asctime)s - %(levelname)s - %(message)s'
)
```

查看后台输出可看到详细日志信息。

---

## 部署指南

### 本地开发环境

```bash
streamlit run stream.py
```

### 服务器部署（使用 Streamlit Community Cloud）

1. 将项目推送到 GitHub
2. 访问 [Streamlit Community Cloud](https://streamlit.io/cloud)
3. 新建应用，选择 GitHub 仓库和 `stream.py`
4. 配置 Secrets（密码、API Key 等）

### 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your.domain.com;

    location / {
        proxy_pass http://localhost:8501;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
```

启动 Streamlit：
```bash
streamlit run stream.py \
  --server.port=8501 \
  --server.headless=true \
  --logger.level=debug
```

---

## 性能优化建议

### 1. 缓存 Token

```python
@st.cache_data(ttl=3600)  # 缓存 1 小时
def get_cached_token():
    return get_news_platform_token()
```

### 2. 并行处理

使用 `ThreadPoolExecutor` 并行下载图片：

```python
from concurrent.futures import ThreadPoolExecutor

with ThreadPoolExecutor(max_workers=5) as executor:
    executor.map(download_image, image_urls)
```

### 3. 压缩输出文件

在 `ppt_ready.py` 中压缩 PPT 文件大小（减少下载时间）

---

## 配置参考

### config.py 完整配置

```python
# ============ 应用配置 ============
APP_PASSWORD = "your_secure_password"

# ============ News Platform 配置 ============
NEWS_AUTH_URL = "https://api.newsplatform.com/oauth/token"
NEWS_CLIENT_ID = "client_id_from_platform"
NEWS_CLIENT_SECRET = "client_secret_from_platform"
NEWS_ARTICLE_URL = "https://api.newsplatform.com/articles"

# ============ AI 服务配置 ============
AI_API_KEY = "your_easyview_ai_key"
AI_API_URL = "https://ai.easyview.com/api"

# ============ 模板配置 ============
TEMPLATE_MAP = {
    "中国大陆": "./template/mainland_template.pptx",
    "香港": "./template/hongkong_template.pptx",
    "新加坡": "./template/singapore_template.pptx"
}

# ============ 目录配置 ============
OUTPUT_DIR = "./ai_generate"
TEMP_DIR = "./temp_workspace"
TEMPLATE_DIR = "./template"
```

---

## 常见问题 (FAQ)

### Q: 如何修改登录密码？

A: 编辑 `config.py`，更改 `APP_PASSWORD` 的值：
```python
APP_PASSWORD = "your_new_password"
```

### Q: 支持多个用户同时生成报告吗？

A: 可以。每个用户的会话是独立的，但建议为每个 `task_id` 配置隔离的文件目录。

### Q: 生成 PPT 需要多长时间？

A: 
- Stage 1 (获取数据)：1-2 秒
- Stage 2 (处理素材)：5-10 秒
- Stage 3 (AI 生成)：1-2 分钟（AI 处理时间）
- Stage 4 (PPT 渲染)：5-10 秒

**总计：约 2-3 分钟**

### Q: 可以自定义模板吗？

A: 可以。编辑 `template/` 目录下的 `.pptx` 文件，然后在 `config.py` 中更新 `TEMPLATE_MAP`。

### Q: 如何处理大量文章？

A: 修改 `construct_json.py` 中的 `MAX_ARTICLES` 参数，或在 `stream.py` 中添加分页逻辑。

---

## 技术栈

- **前端框架**: Streamlit 1.28+
- **后端集成**: FastAPI / 自定义模块
- **数据处理**: Python JSON、Pandas
- **文件操作**: python-pptx、Pillow
- **网络请求**: requests

---

## 许可证

MIT License

---

## 技术支持

如有问题或建议，请：

1. 查看后台日志
2. 检查 `config.py` 配置
3. 验证外部 API 可用性
4. 联系开发团队
